server:
  port: 2000
spring:
  application:
    name: yifan-colud-gateway
  cloud:
    nacos:
      discovery:
        server-addr: yifan.server:8848
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: false
  redis:
    host: yifan.server
    port: 6379
application:
  security:
    oauth:
      ignored:
        - '/yifan-oauth-server/**'
management:
  endpoints:
    web:
      exposure:
        include: '*'



